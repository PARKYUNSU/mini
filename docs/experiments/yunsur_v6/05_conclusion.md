# 05. 재측정 · 결론

재측정일 2026-09-24 08:17 → 09:21 (63분). 프로토콜: [`scripts/eval_local_llm_failure.py`](../../../scripts/eval_local_llm_failure.py) `--repeats 3 --quality`, 36문항 × 3회 (n=108), 로컬 1차 호출만. 실행 [`scripts/eval_v6_all.sh`](../../../scripts/eval_v6_all.sh). 원본 [`04_eval_v6/`](04_eval_v6/).

평가 세트 sha256 `a1cfe0f7f562` 로 §3 기준선과 동일, 판정기도 동일. base(코딩 11.1%)는 §3 고정값을 쓰고, v5 는 같은 밤에 한 번 더 재서 노이즈 대조로 병기한다.

## 결과 — strict 실패율 (hard) · 중앙값

| 슬롯 | base (09-23 고정) | v5 (09-23 고정) | v5 (09-24 같은 밤) | **v6 (09-24)** |
|---|---|---|---|---|
| 잡담 | 0% (0) · 4.6s | 0% (0) · 4.9s | 0% (0) · 6.3s | **0%** (0) · 6.0s |
| 계획 | 0% (0) · 5.2s | 0% (0) · 5.5s | 0% (0) · 6.4s | **0%** (0) · 5.1s |
| RAG | 12.5% (0) · 35.4s | 0% (0) · 32.2s | 0% (0) · 37.6s | **0%** (0) · 30.9s |
| 코딩 | 11.1% (8.3%) · 4.4s | 25% (22.2%) · 5.7s | 19.4% (19.4%) · 5.3s | **22.2%** (16.7%) · 6.3s |
| **전체** | 6% (3%) | 8% (7%) | 6% (6%) | **7%** (6%) |

코딩 난이도별 (기록용): base 0/18 · 3/12 · 1/6 → v5(09-23) 4/18 · 3/12 · 2/6 → v5(09-24) 4/18 · 3/12 · 0/6 → **v6 5/18 · 3/12 · 0/6**.

## 판정 (§3 사전 선언 기준)

| 기준 | 결과 |
|---|---|
| 통과: 코딩 ≤16.1% **and** 다른 슬롯 가드 | ❌ 코딩 22.2% |
| 부분 통과: 코딩 ≤12.5% | ❌ |
| 미달: 16.1% 초과 30% 미만 | ✅ **해당** |
| 실패: 코딩 ≥30% 또는 다른 슬롯 악화 | ❌ (슬롯 가드는 전부 충족 — 잡담·계획·RAG 모두 0%) |

**→ 미달.** 다른 슬롯은 전부 0% 로 가드를 여유 있게 지켰고, 코딩만 목표에 못 미쳤다.

## 가설 판정 — 조작은 성공했고 예측은 틀렸다

| 가설 | 결과 | 근거 |
|---|---|---|
| H1-기전: 이스케이프를 고치면 literal `\n` 유출이 사라진다 | ✅ **확인 (완전)** | v6 응답의 literal `\n`: 잡담 0/24 · RAG 0/24 · 계획 0/24 · 코딩 0/36. 같은 밤 v5 는 3/24 · **24/24** · 0/24 · 3/36 |
| H1-예측: 그 결과 `coding_10` 3건이 통과해 코딩이 약 16.7% 로 내려간다 | ❌ **반증** | `coding_10` 은 v6 에서도 **3/3 `syntax` 실패**. 코딩 22.2% |
| H2: 남은 6건(자기 수정)은 움직이지 않는다 | ✅ **확인** | `coding_04` 1건 · `coding_06` 3건이 같은 밤 v5 와 문항·횟수까지 동일 |
| H3: 잡담·계획·RAG 는 v5 수준 유지 | ✅ **확인** | 셋 다 0%. RAG 속도도 37.6s → 30.9s |

단일 변수 조작 자체는 의도대로 정확히 작동했다. **틀린 것은 그 변수가 코딩 실패의 원인이라는 진단이었다.**

## 왜 틀렸나 — 한 문항에 결함이 두 개 겹쳐 있었다

v5 의 `coding_10` 응답은 이런 모양이었다.

```
```pythonimport ast\ns = 'name=kim;age=30;city=seoul'\ndict_result = {...}
```

여기에는 결함이 **둘** 있다. (1) 개행이 literal `\n`, (2) 펜스 여는 줄 ` ```python ` 과 첫 코드 줄이 **붙어 있음**. v5 결론에서 (1)만 보고 원인으로 지목했는데, `ast.parse` 를 깨뜨린 것은 (2)였다.

v6 는 (1)을 고쳤고 실제로 사라졌다. 그런데 (2)가 남아 같은 문항이 같은 횟수로 실패한다.

```
```pythonimport re

data_str = 'name=kim;age=30;city=seoul'
```

(개행은 이제 진짜 개행이다. 그러나 ` ```python ` 뒤 개행만 여전히 없다 → 추출기가 펜스를 못 찾고 `code_unwrap=plain` 으로 전체를 코드로 넘겨 `syntax`.)

### 붙은 펜스는 파인튜닝이 만든 결함이다

| | 붙은 펜스 (` ```python ` 뒤 개행 없음) |
|---|---|
| 학습 데이터 v5·v6 코딩 300건 | **0건** |
| base 코딩 응답 | **0/36** |
| v5 (09-23) | 3/36 — 전부 `coding_10`, 전부 실패 |
| v5 (09-24) | 3/36 — 동일 |
| **v6 (09-24)** | **3/36 — 동일** |

학습 데이터에 없고 base 도 하지 않는데 파인튜닝 모델만 한다. 3회 측정 × 2모델에서 문항까지 완전히 재현된다.

발동 조건도 좁혀진다. **코드가 `import` 로 시작할 때만** 일어난다.

| | 코딩 응답 중 `import` 로 시작 | 그중 펜스가 붙은 것 |
|---|---|---|
| base | **0/36** | — |
| v6 | 3/36 | **3/3** |

학습 데이터 코딩 300건 중 70건(23.3%)이 `import`/`from` 으로 시작하고 **전부 정상 형식**이다. 즉 파인튜닝은 "라이브러리를 먼저 부르는" 습관을 가르쳤고(base 는 이 문항들에서 한 번도 그러지 않는다), 그 습관이 발동할 때마다 펜스 개행이 빠진다. 데이터를 베낀 것이 아니라 생성 시점에 생기는 결함이다.

## 이스케이프 수정이 실제로 한 일 — 판정에는 안 잡히지만 운영에는 중요하다

RAG 슬롯 응답의 literal `\n`:

| 모델 | RAG 응답 |
|---|---|
| `yunsur_v4` (어댑터, **운영 중**) | **24/24** |
| `yunsur_v4_merged` | 24/24 |
| `yunsur_v5` | 24/24 |
| **`yunsur_v6`** | **0/24** |
| base | 0/24 |

v3 부터 상속된 이 결함은 **v6 에서 처음 사라졌다.** 실패율로는 드러나지 않는다 — 판정기가 literal `\n` 을 보지 않고, RAG 슬롯은 네 모델 모두 0% 이기 때문이다.

소비 경로를 보면 영향이 갈린다.

- 텔레그램 RAG 경로([`apps/telegram_bot/rag_engine.py`](../../../apps/telegram_bot/rag_engine.py))는 `normalize_model_newlines()` 로 세 군데에서 이미 풀고 있다 — 사용자에게 보이는 버그는 아니었다. 누군가 이미 우회해 둔 것이다.
- **에이전트 그래프의 RAG 답변 경로**([`core/graph/agent_nodes.py`](../../../core/graph/agent_nodes.py), `get_rag_answer_llm`)에는 **그 정규화가 없다.** 이 경로로 나가는 답변에는 `\n` 이 글자 그대로 남는다.

## 측정 노이즈 기록

같은 v5 를 이틀에 걸쳐 잰 결과가 코딩 25% → 19.4% 로 5.6%p 움직였다. 난이도별로 보면 **쉬움 4/18 · 보통 3/12 는 두 밤 모두 정확히 동일**하고, 차이는 전부 **어려움 구간 2/6 → 0/6** 에서 났다. 6건짜리 구간이 흔들린다.

§3 의 ±5%p 판정선은 이 폭과 겹친다. 이번 판정(코딩 22.2%)은 통과선 16.1% 와 6.1%p 차이라 노이즈만으로 뒤집히기는 어렵지만, **경계에 가까운 결과였다면 판정을 신뢰할 수 없었다.** RAG 가드 걱정(base 가 1/24↔3/24 로 흔들린 이력)은 이번엔 현실화되지 않았다 — v5·v6 모두 RAG 0%.

## 운영 결정

- **로컬 기본은 base `qwen3.5:9b` 유지.** 전체 6%(hard 3%)로 여전히 가장 낫다.
- **RAG 답변 슬롯은 `yunsur_v4` → `yunsur_v6` 으로 교체할 것.** 실패율은 v4·v5·v6 모두 0% 로 같지만, v6 만 literal `\n` 을 내지 않는다. 특히 에이전트 그래프 경로에는 우회 정규화가 없으므로 교체가 곧 수정이다. 속도도 30.9s 로 가장 빠르다.
- 교체 후 [`apps/telegram_bot/rag_engine.py`](../../../apps/telegram_bot/rag_engine.py) 의 `normalize_model_newlines()` 는 **지우지 말 것.** 멱등이라 해가 없고, v4 로 되돌릴 여지가 남는다.
- **코딩 슬롯은 계속 base.** 파인튜닝 모델 전부 base(11.1%)보다 나쁘다.

## 다음 라운드(v7)에 반영할 것

1. **붙은 펜스를 직접 겨냥한다.** 원인이 생성 시점이므로 데이터 필터로는 안 된다. 두 갈래가 있다: (a) 학습 데이터 코딩 샘플의 `import` 비중(23.3%)을 낮춰 "라이브러리 먼저" 습관 자체를 누른다, (b) `code_extract` 가 ` ```python ` 뒤 개행 누락을 복구하도록 고친다. (b)는 데이터 실험이 아니라 코드 수정이므로 **v7 과 분리해 먼저 적용**하고, 그 뒤 재측정으로 이 결함의 기여분을 정확히 뗀다.
2. **어려움 구간 문항을 2 → 6 으로 늘린다.** 6건짜리 구간이 판정 노이즈의 유일한 출처였다.
3. RAG·잡담·계획은 건드리지 않는다 — 3라운드 연속 0% 또는 그에 준함.
4. 이스케이프 수정은 유지한다 (v6 데이터를 v7 기반으로).

## 실험 과정에서 얻은 교훈 (실패 기록)

- **한 문항에 결함이 겹쳐 있으면 실패 원문만으로는 원인을 못 가른다.** v5 의 `coding_10` 에는 이스케이프와 붙은 펜스가 동시에 있었고, 눈에 띄는 쪽을 원인으로 골랐다. 단일 변수 실험이 이것을 드러냈다 — 조작은 완벽히 작동했는데 결과가 안 움직이면, 진단이 틀린 것이다.
- **조작의 성공과 가설의 성공을 따로 기록한다.** H1 을 "기전"과 "예측"으로 나눠 적었더니, 데이터 수정이 의도대로 작동했다는 사실(운영 결정의 근거)이 예측 실패에 묻히지 않았다.
- **학습 전 소급 감사가 한 라운드를 구했다.** 원래 v6 의 주된 변수였던 코딩 필터는 v5 생성분 307건 중 1건만 걸러, 학습 전에 철회했다 (§4).
- **판정 분류는 빈 구간 없이 닫아 둔다.** 예상값이 통과선과 실패선 사이에 떨어지는 것을 결과 전에 발견해 '미달'을 추가했고, 실제 결과가 정확히 그 구간이었다.
