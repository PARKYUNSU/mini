# 실험 노트 — yunsur_v6: RAG 이스케이프 결함 수정 (단일 변수)

- 시작일: 2026-09-23
- 선행 실험: [`../yunsur_v5/`](../yunsur_v5/) — 판정 **실패**. 계획은 성공(16.7%→4.2%, H1 확인), 잡담·RAG 유지(H3 확인), **코딩은 반증**(16.7%→22.2%, H2 반증). [`05_conclusion.md`](../yunsur_v5/05_conclusion.md)
- 베이스 모델: `Qwen/Qwen3.5-9B`. 배포는 머지 ([`scripts/merge_lora_gguf.sh`](../../../scripts/merge_lora_gguf.sh))
- **범위: 단일 변수.** RAG 학습 데이터의 literal `\n` 이스케이프 결함 수정 하나뿐이다. v5 학습 파일 1,400건 중 **RAG 175건만 바뀌고 나머지 1,225건은 바이트 단위로 같다** → v6와 v5의 차이는 이 수정 하나로 귀속된다. 생성 파이프라인·LLM이 필요 없다 ([`scripts/gen_v6_dataset.py`](../../../scripts/gen_v6_dataset.py) `--build`)
- **빠진 것: 코딩 거절 필터.** 원래 v6의 주된 변수였으나 소급 감사에서 무력함이 드러나 뺐다 (§4)
- 비교 대상: `yunsur_v6` / `yunsur_v5` / `qwen3.5:9b`(base) — **3모델**
- 측정 도구: [`scripts/eval_local_llm_failure.py`](../../../scripts/eval_local_llm_failure.py) `--repeats 3 --quality`
- 실패 정의·판정기: v5와 동일 (잡담 펜스 예외 포함). 보고 숫자는 **strict(hard+quality)**, hard는 괄호 병기

## 1. 문제 정의

v5의 코딩 실패 4건은 원인이 v4 진단과 달랐다.

v4에서는 "코드 안 긴 한국어 주석 → 토큰 상한(800)에서 잘림"으로 보고, v5에서 **주석 비율 >30% 거절** 필터를 넣었다. 필터는 데이터에서 작동했다 — 코딩 주석 비율 p95 0.42 → 0.16, 학습 토큰 잘림 0건. 그런데 실패율은 16.7% → 22.2%로 오히려 올랐다.

실패 원문을 보면 행동이 다르다.

```
fibonacci.append(sum(fibonacci[-2:]) if len(fibonacci) >= 2 else None # 단순화 로직 수정 필요 없음 (다음 코드 참조)

# 올바른 구현으로 재작성 및 출력합니다.

def generate_fibo(n):
    fib = [0, 1] * n + []
```

구현을 한 번 쓴 뒤 **주석으로 자기 코드를 의심하고 두 번째 구현을 시작**한다. 그러다 상한에 걸려 표현식 중간에서 잘리고 `syntax` 또는 펜스 미닫힘이 된다. 주석은 이 행동의 흔적이지 원인이 아니다. 같은 패턴이 `yunsur_v4_merged` 에도 있으므로 **베이스에서 온 습관**이다.

### 평가 세트가 이 문제를 가리고 있었다

현재 코딩 6문항 중 5개가 한 줄짜리다.

| id | 요청 | 성격 |
|---|---|---|
| coding_01 | 1부터 10까지 합 | 한 줄 |
| coding_02 | 리스트 정렬 | 한 줄 |
| coding_03 | 문자열 길이 | 한 줄 |
| coding_04 | 1~20 짝수 출력 | 한 줄 (반복문) |
| coding_05 | 딕셔너리 값 합 | 한 줄 |
| coding_06 | 피보나치 앞 10개 | **루프 + 상태** |

v5의 코딩 실패 4건은 `coding_06`(3/3 전부)과 `coding_04`(1회)에서 나왔다. 즉 "코딩 22%"는 사실상 **유일하게 사소하지 않은 문항이 매번 실패한다**는 뜻이고, 지금 표본으로는 "슬롯 전반의 악화"와 "한 유형의 재현성 있는 실패"를 구분할 수 없다.

## 2. 가설

- **H1: v5 코딩 실패의 일부는 RAG 데이터에서 새어 나온 이스케이프 습관이다.** → RAG 175건의 literal `\n` 을 진짜 개행으로 고치면 응답의 literal `\n` 유출이 사라지고, `coding_10` 3건이 통과해 코딩 strict 가 25% → 약 16.7% 로 내려간다. RAG 슬롯 자체(0%)는 나빠지지 않는다.
- **H2: 남은 코딩 실패 6건은 이 수정으로 움직이지 않는다.** → `coding_06` 류의 자기 수정 재작성은 학습 데이터에 없는 행동이므로, 이스케이프 수정과 무관하게 남는다. (남으면 H1의 범위가 확인되고, 같이 줄면 두 현상이 같은 뿌리라는 단서가 된다.)
- **H3: 잡담·계획·RAG 는 v5 수준을 유지한다.** (데이터를 건드리지 않았고, RAG 는 표기만 고쳤다)

하이퍼파라미터는 v3 = v4 = v5 = **v6로 계속 고정** (LoRA r16/α32, lr 2e-4, 1 epoch, seq 2048, adamw_8bit, seed 3407). 슬롯별 건수도 400/300/400/300 그대로. **바뀌는 것은 RAG 175건의 개행 표기 하나뿐이다.**

난이도 구간별 실패율은 판정에 넣지 않고 기록만 한다 (문항 수가 적다).

## 3. 성공 기준 (사전 선언 — 결과를 본 뒤 바꾸지 않는다)

평가 세트가 30 → 36문항으로 바뀌므로 **v5 라운드의 숫자와 직접 비교할 수 없다.** 따라서 기준을 절대 수치가 아니라 **같은 세트에서 재측정한 base·v5에 대한 상대 규칙**으로 선언한다. base와 v5는 **v6 학습 전에** 새 세트로 재측정하고 그 숫자를 고정한다 (§5).

같은 36문항 × 3회 (n=108), 같은 프롬프트, 3모델을 같은 밤에 측정한 결과에서:

- **통과**: v6 코딩 strict가 **base 코딩 strict + 5%p 이내**이고, 잡담·RAG·계획이 **v5 대비 +5%p 이내**.
- **부분 통과**: v6 코딩 strict가 **v5 코딩 strict의 절반 이하**이고, 다른 슬롯이 v5 대비 +5%p 이내 → "H1 유효, base 도달은 미달"로 기록.
- **실패**: v6 코딩이 v5보다 **+5%p 이상 악화**, 또는 다른 슬롯이 v5 대비 **+5%p 이상 악화**.

판정 예외는 v5와 동일하다 (잡담 펜스 예외 `TEMPLATE_REQUEST_RE`). 3모델에 같은 판정기를 쓴다.

### 기준값 (2026-09-23 08:02→09:11, 68분, 36문항 n=108, 판정기 커밋 `fe20b5a`, 세트 sha256 `a1cfe0f7f562`)

| 슬롯 | base strict (hard) · 중앙값 | v5 strict (hard) · 중앙값 |
|---|---|---|
| 잡담 | 0% (0) · 4.6s | 0% (0) · 4.9s |
| 계획 | 0% (0) · 5.5s | 0% (0) · 5.5s |
| RAG | 12.5% (0) · 30.9s | **0%** (0) · 32.2s |
| 코딩 | **11.1%** (8.3%) · 4.4s | 25% (22.2%) · 5.7s |
| 전체 | 6% (3%) | 8% (7%) |

코딩 난이도별 (기록용): base 쉬움 0/18 · 보통 3/12 · 어려움 1/6 → v5 쉬움 4/18 · 보통 3/12 · 어려움 2/6.

**위 표에서 파생되는 v6 판정선** (이제 절대 수치로 고정한다):

- **통과**: v6 코딩 strict **≤ 16.1%** (base 11.1% + 5%p) **and** 잡담 ≤5% · 계획 ≤5% · RAG ≤5% (각각 v5 + 5%p)
- **부분 통과**: v6 코딩 strict **≤ 12.5%** (v5 25% 의 절반) **and** 다른 슬롯이 위 상한 이내
- **실패**: v6 코딩 **≥ 30%** (v5 + 5%p), 또는 다른 슬롯이 v5 대비 +5%p 이상 악화

- **미달**: 위 세 조항에 모두 해당하지 않을 때 (코딩 strict 가 16.1% 초과 30% 미만) → "개선했으나 기준 미충족"으로 기록한다.

조항은 통과 → 부분 통과 → 미달 → 실패 순으로 본다. `미달` 은 v6 결과를 보기 전(2026-09-23)에 분류를 완결시키려고 추가한 것이다 — 문턱값은 건드리지 않았고, H1 의 예상값 16.7% 가 기존 세 조항 사이의 빈 구간에 떨어지는 것을 발견해 메웠다. base 코딩이 0%가 아니라 11.1%로 나오는 바람에 **통과선(16.1%)이 부분 통과선(12.5%)보다 느슨해졌다.** 규칙 자체는 v6 결과를 보기 전에 선언한 것이므로 그대로 두고, 이 역전은 해석에서 감안한다.

**판정 기준에 넣지 않고 기록만 하는 것**: 난이도 구간별(쉬움/보통/어려움) 실패율, 중앙값 응답시간, 실패 응답의 코드 블록 개수. H2는 이 기록으로 판단하되 통과/실패 판정에는 쓰지 않는다 — 난이도 구간은 문항 수가 적어 판정에 쓸 만큼 안정적이지 않다.

## 4. 데이터 설계

| 슬롯 | v5 | **v6** | 출처 | 변경 |
|---|---|---|---|---|
| RAG | 400 | **400** | v5 파일 + **이스케이프 수정** | literal `\n` → 진짜 개행 (175건) |
| 잡담 | 300 | **300** | v5 파일 그대로 | 없음 |
| 계획 | 400 | **400** | v5 파일 그대로 | 없음 |
| 코딩 | 300 | **300** | v5 파일 그대로 | 없음 (필터 교체는 §1 대로 철회) |
| 합계 | 1,400 | **1,400** | | |

### 코딩 거절 필터는 무력했다 — 그래서 범위에서 뺐다

v6는 원래 코딩 거절 필터를 `comment_heavy` → `multi_block` + `self_revision` 으로 갈아끼우는 것이 주된 변수였다. 학습 전에 [`scripts/gen_v6_dataset.py`](../../../scripts/gen_v6_dataset.py) `--audit` 으로 v5 생성분에 소급 적용해 보니, **통과분 307건 중 거절이 1건**이고 `train_data_v5` 에 들어간 코딩 300건에는 **0건**이었다.

자기 수정 행동은 **학습 데이터에 없다.** 추론 시점에 모델이 만들어내는 것이므로 데이터 필터로는 바꿀 수 없다. H1을 설계대로 검증할 방법이 없어 범위에서 뺐다.

슬롯별 대조가 다음 가설을 시사한다 (v6에서 검증하지 않는다).

| 슬롯 | 자기 수정 표현 | output 중앙값 | 학습 비중 |
|---|---|---|---|
| RAG | 29/400 = **7.2%** | **1,411자** | 29% |
| 잡담 | 7/300 = 2.3% | 75자 | 21% |
| 계획 | 1/400 = 0.2% | 156자 | 29% |
| **코딩** | **0/300 = 0%** | 98자 | 21% |

코딩 데이터는 깨끗하고, 길이가 14배인 RAG가 학습의 29%를 차지한다. literal `\n` 유출이 RAG→코딩으로 번진 것은 확인됐으므로 자기 수정도 같은 경로일 수 있으나 **미증명이다.** → v7 질문: RAG의 길이·비중이 다른 슬롯의 서술 습관을 만드는가.

**교훈**: v5 결론에 "원인 진단은 실패 원문에서 나와야 한다"고 적었는데, 이번엔 반대 실수를 했다. 원문에서 행동을 특정하고 그것을 데이터 필터로 옮겼지만, **그 행동이 데이터에 있는지는 확인하지 않았다.** 감사를 학습 전에 돌린 것이 비용을 막았다.

### RAG 이스케이프 수정

v5 학습 데이터의 RAG 400건 중 **175건(44%)** 이 개행을 literal `\n`(역슬래시+n 두 글자)으로 담고 있다. 출처는 전부 `v3_clean` 이고, v4 에도 164건 있었다 — v3 파이프라인에서 이스케이프가 한 번 더 먹은 것이 그대로 상속됐다.

```
'## 서론\\n대규모 언어 모델(LLM)이 128K에서 1M 토큰에...'
```

기준선 측정의 대조가 원인을 확정한다. **base 는 108건 전부 literal `\n` 을 쓰지 않는데, v5 는 RAG 24/24 전부, 잡담 3/24, 코딩 3/36 에서 쓴다.** 학습으로 생긴 습관이고, 코딩에서는 코드 추출이 깨져 `syntax` 실패가 된다.

RAG 슬롯이 0% 라서 이 결함이 가려져 있었다 — 판정기가 literal `\n` 을 보지 않으므로 RAG 에서는 무해해 보이고, 피해는 다른 슬롯에서 난다.

수정은 RAG 샘플의 `output` 에서 이스케이프된 개행만 되돌린다. **코딩 샘플의 literal `\n` 2건은 건드리지 않는다** — `sep='\n'`, `text.replace("\n","")` 처럼 파이썬 코드 안의 진짜 문자열이라 정상이다.

**판정기는 바꾸지 않는다.** literal `\n` 을 품질 실패로 추가하면 §3 의 기준선을 다시 재야 한다. 대신 **응답의 literal `\n` 빈도를 기록 지표로 둔다** (결과 jsonl 에서 사후 집계, 판정 아님).

### 코딩 거절 필터 교체 — 철회

`comment_heavy` 해제 + `multi_block`·`self_revision` 추가를 계획했으나 위 절대로 철회했다. 조사 기능은 `gen_v6_dataset.py --audit` 에 남아 있다.

## 5. 평가 세트 변경 (36문항)

코딩 6 → **12문항**. 나머지 슬롯(잡담 8 / RAG 8 / 계획 8)은 건드리지 않는다.

| 구간 | 문항 | 성격 |
|---|---|---|
| 쉬움 | `coding_01`~`coding_06` | 기존 유지. 5개는 한 줄, `coding_06`만 루프+상태 |
| 보통 | `coding_07` 누적합 · `coding_08` FizzBuzz · `coding_09` 글자 수 집계 · `coding_10` 문자열→딕셔너리 파싱 | 루프+상태, 조건 분기, 딕셔너리 집계, 문자열 파싱 |
| 어려움 | `coding_11` `second_largest`(중복·길이 부족 시 None) · `coding_12` 중첩 자료구조 평균 | 함수 정의 + 경계 조건, 중첩 자료구조 처리 |

문항은 전부 **자기완결**이다 — 파일·네트워크·`input()` 없이 그대로 실행돼야 한다 (`_coding_ok` 가 임시 디렉터리에서 8초 제한으로 실제 실행한다).

- 새 6문항은 학습 데이터(v5 코딩 raw + v6 재생성분)와 **유사도 ≥0.72 이면 제외**한다 — v4·v5와 같은 격리 규칙.
- 판정기(`_coding_ok`: `ast.parse` + 금지 호출)는 **바꾸지 않는다.** 난이도를 올리되 채점 방식은 고정한다.
- 세트를 바꾼 직후 **base·v5를 새 36문항으로 재측정**하고 그 숫자를 `04_eval_v6/` 에 고정한다. v6 학습·측정은 그 다음이다. (약 2시간, 맥미니)

## 6. 학습

v5와 동일. [`scripts/train_lora.py`](../../../scripts/train_lora.py) → RunPod Pod ([`scripts/runpod_train.sh`](../../../scripts/runpod_train.sh), L40S 48GB · 볼륨 50GB · 컨테이너 디스크 60GB) → HF private 업로드 → 맥미니 `merge_lora_gguf.sh v6`.

v5 실행에서 확인된 주의점이 스크립트 주석에 들어가 있다 — 이미지 핀은 썩으니 태그 존재를 먼저 확인할 것, `KEEP_POD=1` 로 정상 종료하면 컨테이너가 재시작돼 학습이 다시 도니 업로드 확인 후 즉시 terminate 할 것.

## 7. 재측정 · 결론

→ `04_eval_v6/`, `05_conclusion.md` (측정 후 작성)

## 체크리스트

- [x] 평가 세트 확장 — `tests/fixtures/local_llm_failure_eval.jsonl` 36문항 (코딩 12). 유사도 격리 확인 완료: 학습 1,400건 대비 최대 0.416, 기존 문항 대비 최대 0.639 (상한 0.72). 참조 답안 6개가 판정기 조건에서 실행 통과
- [x] 맥미니: base·v5를 새 36문항으로 재측정 ([`scripts/eval_v6_baseline.sh`](../../../scripts/eval_v6_baseline.sh), 68분) → [`04_eval_v6/`](04_eval_v6/), 판정선 §3 에 고정
- [x] `scripts/gen_v6_dataset.py` — `--build` 로 v5 파일에서 RAG 이스케이프만 수정. 1,400건 중 RAG 175건·3,066개 개행 복원, 나머지는 v5 와 동일
- [x] 이스케이프 수정 검증 — RAG 400건 literal `\n` **0건**, 코딩의 정상 `\n` **2건 보존**, 바뀐 행은 rag 175건뿐, RAG 진짜 개행 4,132 → 7,198
- [x] v5 코딩 raw 에 새 필터 소급 적용 → **307건 중 1건만 거절** → 필터 철회 (§4)
- [x] `.gitignore` v6 예외 추가
- [ ] RunPod Pod 생성 → HF `yunsur_v6_lora` 업로드
- [ ] 맥미니: `hf download` → `merge_lora_gguf.sh v6`
- [ ] 3모델(v6/v5/base) 같은 밤 재측정 → `05_conclusion.md`

## 진행 로그

| 날짜 | 내용 |
|---|---|
| 2026-09-23 | **코딩 필터 철회 → v6 를 단일 변수로 재설계.** 학습 전에 `gen_v6_dataset.py --audit` 으로 새 필터를 v5 생성분에 소급 적용했더니 307건 중 1건만 걸렸다 — 자기 수정 행동은 학습 데이터에 없고 추론 시점에 생긴다. 슬롯별 대조(RAG 자기수정 7.2%·중앙값 1,411자 vs 코딩 0%·98자)는 RAG 의 길이·비중이 원인일 가능성을 시사하나 미증명 → v7 질문. v6 는 **RAG 이스케이프 수정 하나**만 한다: `--build` 로 1,400건 중 RAG 175건(3,066개 개행)만 바뀌고 나머지 1,225건은 v5 와 동일, 검증 전항목 통과. 생성·LLM 불필요. |
| 2026-09-23 | **literal `\n` 원인 규명 → 범위를 변수 2개로 확대.** v5 학습 데이터 RAG 400건 중 **175건(44%)** 이 개행을 literal `\n` 으로 담고 있고 출처는 전부 `v3_clean`(v4 에도 164건). 평가에서 base 는 108건 전부 0, v5 는 RAG 24/24·잡담 3/24·코딩 3/36 — 학습으로 생긴 습관이 코딩 추출을 깨뜨린다. 코딩 학습 데이터의 `\n` 2건은 파이썬 코드 안의 정상 문자열이라 코딩 데이터는 깨끗하다. 이스케이프만 고쳐도 코딩 25%→16.7% 예상(통과선 16.1%), 남은 6건이 자기 수정 재작성. 판정기는 바꾸지 않는다(기준선 유지). |
| 2026-09-23 | **기준선 측정 완료** (68분, n=108). base 코딩 **11.1%** — 옛 6문항에서 0%였던 것이 난이도를 넣자 드러났다. base 실패는 `coding_10` 3/3(runtime)과 `coding_12` 1건. `coding_10` 은 문항 결함이 아니다: 요청하지도 않은 타입 변환을 넣어 `int('kim')` 에서 ValueError 를 낸다. v5 코딩 25%, 그중 쉬움 4/18 은 v5 라운드의 `coding_04`×1·`coding_06`×3 을 그대로 재현했다. **새로 드러난 실패 양상**: v5 의 `coding_10` 3건은 로직이 맞는데 개행을 literal `\n` 으로 써서 추출·파싱이 깨진다 — v6 가 겨냥한 자기 수정 재작성과 다른 문제다. 또 base RAG 가 v5 라운드 4.2%(1/24) → 이번 12.5%(3/24) 로 흔들려, ±5%p 판정선이 측정 노이즈와 겹칠 수 있다. |
| 2026-09-23 | 평가 세트 30→36 확장 (코딩 6→12). 새 6문항 유사도: 학습 데이터 최대 0.416, 기존 문항 최대 0.639(`coding_09`↔`coding_06`) — 상한 0.72 아래. 참조 답안으로 `ast.parse`+8초 실행 통과 확인. **다음: base·v5를 새 36문항으로 재측정해 기준 숫자 고정.** |
| 2026-09-23 | 실험 노트 개설, 성공 기준 사전 선언. v5 결론에서 코딩 실패 메커니즘을 자기 수정 재작성으로 특정했고, 평가 세트 6문항 중 5개가 한 줄짜리라 판정이 `coding_06` 한 문항에 걸려 있었음을 확인 → 평가 세트 30→36 확장과 코딩 필터 교체를 v6 범위로 확정. 범위는 코딩 슬롯 단독. |
